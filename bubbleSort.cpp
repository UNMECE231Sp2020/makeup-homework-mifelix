#include "sort.h"

void swap(int *x, int *y)
{
	int temp = *x;
	*x = *y;
	*y = temp;
}

void bubbleSort(int *begin, const int *end)
{
	int k;
	int p;
	int h = k+1;
	int baby = k!=end;
	int yoda = p!=end;

	for(k=begin, baby,k++) {
	
	 	for(p=h; yoda, p++) {
	       		

			if(*k > *(h)) {
				
				swap(k, h); //used sawp right here.
			}
		}
	}
}
	
	
	
	
	
	
	
	
	
	//Use swap to exchange two values
}
